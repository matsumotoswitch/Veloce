# Veloce Project Rules & Agent Guidelines

## 0. 絶対遵守事項 (ABSOLUTE RULES)
- **【厳禁】勝手なコミット**: ユーザーからの明確な指示（「コミットして」等）がない限り、AIが自律的に `git commit` を実行することは**絶対に行わないこと**。
- 修正したコードは常にワーキングディレクトリの「未コミット状態」に留め、ユーザー自身が差分を確認・テストした上で手動でコミットできるようにする。

## 1. 対象環境と技術スタック
- **動作ターゲット**: Windows 8.1 をサポート範囲に含める。
  - *注記: これは現作者のローカル環境の都合によるものです。フォークした他の開発者はこの制約を守らなくても構いません。*
- **技術制約 (WebView2 v109)**: Windows 8.1環境で動作させるため、Chromium 109 互換の JavaScript / CSS を出力すること（v110以降の新しい配列メソッドやCSSネストなどは使用厳禁）。
- **技術スタック**: Tauri (Rust) + SQLite + Vanilla JS のみを使用する。React等の仮想DOMフレームワークや肥大化するライブラリの導入は禁止。
- **処理対象となる画像ファイル**: NovelAIユーザをメインターゲットとし、処理する画像形式はpngやwebpがメインとなる。パフォーマンスチューニングもそれらがメインとなる。jpgなどそれ以外の画像形式は、「WebViewが対応している」「一般的な画像形式である」ということで対応しているだけなので、そちらに注力することは禁止。
- **コア最適化アーキテクチャ**:
  - Rust側のメモリ管理にはグローバルアロケータとして `mimalloc` を使用すること。
  - SQLiteは高速化設定（`journal_mode = WAL`, `mmap_size`, `temp_store = MEMORY` 等）を前提に実装すること。
  - ハッシュ処理が必要な場合、暗号学的ハッシュ関数は避け、超高速な非暗号学的ハッシュ `xxHash (xxh3_64)` を使用すること。

## 2. UI / デザインガイドライン
- **絵文字の厳禁**: Windows 8.1 で正常に表示できないため、UI上（およびAIのチャット出力等）への絵文字（⭐など）の使用は厳禁とする。
- **アイコンの使用**: アイコンなどが必要な場合は、絵文字ではなく SVG アイコンを使用し、他のUIコンポーネントとテイストを統一すること。

## 3. パフォーマンスとアーキテクチャ設計
- **極限の軽量化**: メモリ割り当てやガベージコレクション（GC）の発生を最小限に抑えること。
- **DOM Poolと再利用**: インスペクターやリスト等で大量のDOM要素を扱う際、要素の都度生成・破棄（GC発生の原因）を行わず、`display = 'none'` や `replaceChildren()` によるDOM要素の使い回し（DOM Poolアーキテクチャ）を徹底すること。
- **DOM操作の最適化**: `DocumentFragment` やイベント委譲（Event Delegation）を活用し、O(1)の差分更新を徹底すること。再計算（Reflow/Repaint）を誘発する非効率なDOM操作は避ける。
  - ループ処理内での `querySelector` は厳禁。あらかじめインデックスが自明な場合は `.children[i]` を用いて O(1) アクセスすること。
  - `style.height` や `textContent` 等の書き込みは、以前の値と異なる場合のみ行うこと（無条件に書き込むとブラウザのReflowがスケジュールされパフォーマンスが低下する）。
- **仮想スクロールと同期レンダリング**: フォルダ切り替えやタブ切り替え時のスクロール位置復元でレンダリングが遅延するのは許容できない。不要な二重レンダリングを防ぐため、コンテナ高さが算出された直後に「同期的」に復元処理を行うこと。
- **メモリフットプリントの最小化**: サムネイル生成等の巨大な Base64 データはJS側に保持し続けず、Rust側に保存後ただちに破棄し、軽量な Tauri Asset URL (カスタムプロトコル等) に差し替えること。
- **動画ストリーミングの制約**: 動画（MP4等）再生用のローカルHTTPサーバーでは、メモリ圧迫を防ぐため、1回のリクエストに対する返却サイズを最大2MBに厳格に制限し、部分配信（`206 Partial Content`）すること。
- **WebView固有の制約への配慮**: スクリプトからの `navigator.clipboard.readText()` はセキュリティプロンプトが表示されるため使用しない。クリップボード状態はJSの内部変数内で管理すること。
- Veloceという名前に恥じないパフォーマンスを維持すること。とくにサムネイル作成や表示については、画像ビューアの根幹となる部分である。ミリ秒以下の単位での処理にすること。

## 4. テストと開発フロー
- **【重要・厳守】** 実装コード（機能追加・修正）を出力する際は、**必ず同一の回答（または同一のステップ）内でペアとなるテストコードも出力すること**。「テストは後で実装します」「〜と同様にテストを追加してください」等の省略・先送り（サボり）は固く禁ずる。
- Rust、JavaScriptを修正したときはテストコードを記載し、既存のコードも含めて実行すること。
- 既存のコードの修正や機能追加の際は、必要に応じて既存のテストコードも修正すること。
- 新規のコードを記載する際は既存のテストコードを参考に、命名規則やコードスタイルを統一すること。
- 不具合として指摘された内容についてはテストに追加し、修正後は必ずテストをクリアすること。
- 新規の機能追加の際は、必ずテストコードも新規に記載すること。
- 編集用に作成したスクリプト（*.cjsなど）は、反映が終わったら削除すること。
- 自己のテスト用に作成したデータファイルは、確認が終わったら削除すること。
- **検証用一時ファイルの徹底削除**: 動作検証のために一時的に作成したテストスクリプトやダミープロジェクトは、検証終了後、元のプロジェクトツリーに混入しないよう必ず削除すること（誤って `src/` 配下等に作成したファイルも残さないよう注意する）。
- **【環境依存の文字化けへの注意 (Mojibake)】** PowerShell の `Get-Content` / `Set-Content` 等を用いたファイルの書き換えは、文字コード（BOMなしUTF-8など）が破壊され日本語の文字化け（Mojibake）を誘発し、コンパイルエラーの原因となるため極力避けること。ファイルの編集には `multi_replace_file_content` ツールか、Pythonの `codecs` 等を使用してエンコーディングを厳密に制御すること。
- **【テストの限界を認識すること】** `vitest` による自動テストは「ロジックの正しさ」のみを保証する。DOMレンダリングのタイミング・requestAnimationFrame・ブラウザのレイアウト計算など、**実機でしか検証できない挙動が存在する**。実機で動作しない場合は「テストはパスしているが実機で動かない」ことを正直に報告し、原因を追究すること。自動テストのパスをもって「動作確認完了」とは言わない。

## 5. リポジトリの運用方針
- 本プロジェクトはAI生成コードを主体として開発を行っているため、外部からの Pull Request (PR) は受け付けていない。

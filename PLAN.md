# Veloce アプリケーション詳細仕様・機能定義書 (PLAN.md)

本書は、AI生成画像に特化した超軽量・高速画像管理マネージャー＆ビューアー「Veloce」の、実際のソースコード構造および実装に基づいた正確な仕様書である。

---

## 1. システムアーキテクチャ・動作環境

### 1.1 技術スタック & コア最適化
* **基本設計思想:** 確実にサムネイル表示が行われ、正常に動作することを大前提とした上で極限の速度を追求する。特定の破損ファイルや環境依存のAPI遅延によって処理が沈黙（ハングアップ）しないよう、タイムアウト制御やフェイルセーフを徹底した堅牢な基盤の上に最適化を構築する。
* **バックエンド:** Rust (Tauri v1)
  * **高効率メモリ管理:** `mimalloc` (`MiMalloc`) をグローバルアロケータに指定し、長時間の画像処理における断片化とメモリ肥大化を徹底的に抑制。
  * **マルチスレッド処理:** `rayon` による並列ソート、および `tokio::task::spawn_blocking` によるI/Oバウンドな非同期タスク分離。
  * **超高速ディレクトリ走査:** 標準の `std::fs::read_dir` ではなく `jwalk` クレートを採用し、並列処理による極めて高速なディレクトリツリー展開を実現。
* **データベース (キャッシュレイヤー):** SQLite (r2d2接続プール接続、最大16スレッド)
  * **高速化設定:** `journal_mode = WAL`, `synchronous = NORMAL`, `mmap_size = 268435456 (256MB)`, `cache_size = -4000 (4MB)`, `temp_store = MEMORY`, `busy_timeout = 5000` を指定し、16スレッドでのメモリ肥大化をOSメモリマッピングに逃がしつつロック競合を防止。
  * **インデックス設計:** パス検索用 `idx_cache_path`、更新日時用 `idx_cache_mtime`、アクセス日時用 `idx_cache_last_accessed`、レーティング用 `idx_ratings_rating` に加え、スマートフォルダ一覧取得をカバーする複合カバリングインデックス `idx_cache_smart_cover_v2 ON cache (path, mtime DESC, size, ctime, width, height)` を配備。
  * **FTS5 全文検索エンジン:** `cache_fts USING fts5(...)` 仮想テーブルを配備し、生成プロンプト・ネガティブプロンプト・生成元の全文検索インデックスを構築。`cache` テーブルへの追加・更新・削除と同期するトリガー (`cache_ai`, `cache_au`, `cache_ad`) により、O(1) の行識別子 (`rowid`) ベースで常に完全同期を維持。
  * **データベースの定期最適化 (`audit_cache`):** 「キャッシュの精査と修復」実行時、非存在ファイル削除、パス正規化による重複排除、欠落寸法・サイズ・タイムスタンプ補完、未生成サムネイルの非同期生成を実施後、自動的に `VACUUM`、`ANALYZE`、および `PRAGMA wal_checkpoint(TRUNCATE);` が走り、不要領域の回収と `-wal` ファイルサイズのゼロ切り詰めを完了する設計。
  * **デバッグログとリリースビルド設計:** 開発・デバッグ時は Windows API の `AttachConsole(ATTACH_PARENT_PROCESS)` を用いて親プロセス（ターミナル）に接続し標準出力を表示。本番用リリースバイナリでは開発者ツール（DevTools）の起動をショートカットキー（F12, Ctrl+Shift+I等）を含めて無効化。
* **フロントエンド:** Vite + Vanilla JS + HTML5 / CSS3
  * **UIフレームワーク不使用:** ReactやVueなどの仮想DOMは一切排除。生のDOM操作と Event Delegation（イベント委譲）、および DOM Pool 構造により極限まで軽量化。
* **ファイルハッシュ:** 暗号学的ハッシュではなく、極めて高速な非暗号学的ハッシュ `xxHash (xxh3_64)` をベースに「`パス_更新日時(mtime)`」から16進数16文字の `hash_key` を生成してキャッシュを一元管理。

### 1.2 動作環境要件
* **OS:** Windows 8.1 環境（WebView2 v109 / Chromium 109 互換）への完全準拠。
* **UI制約:** 文字化けや環境依存を防ぐため、システム内での絵文字（⭐や✖など）の使用は厳禁とし、すべて高品質な **SVGアイコン（`ICON_SVGS`）で代替**。
* **JS/CSS制約:** 古いChromium互換のため、最新のECMAScript配列メソッドやCSSネストなどの利用制限を遵守。

---

## 2. コア機能詳細

### 2.1 独立マルチビューアーウィンドウ
* **ウィンドウ制御 (Window Pool):** メイン画面から完全に独立した子ウィンドウを展開。ウィンドウの都度生成・破棄によるWebView2の起動遅延やGC負荷を防止するため、既存の非表示ウィンドウを再利用する Window Pool パターンを採用。セッション開始時に `viewer-init-session` イベントでインデックス・パス・総件数を安全に同期して初期化。多重起動時はショートカット（`A` キー）でモニターの有効ワークエリアに合わせて**横一列に素早く自動整列**。
* **Windows 8.1 前面化ハック:** 整列時、バックエンド側で別スレッドから `SetForegroundWindow` および `SetWindowPos (HWND_TOPMOST)` をミリ秒遅延させて呼び出し、古いOS特有のウィンドウフォーカス奪取問題を物理的に解決。
* **メディア表示とフィルター処理:** 静止画像に加え動画（MP4等）の再生に対応し、メディア種別に応じた自然寸法（`videoWidth` / `naturalWidth`）を正確に取得。拡大縮小、回転（左右90度）、水平・垂直反転、等倍（100%）表示、SVGフィルター定義（`feConvolveMatrix` によるアンシャープマスク）を用いた小数倍率拡大時の輪郭鮮鋭化に対応。
* **ダブルバッファリング（DOM Swap）描画:** ビューアーの画像切り替え時、直接 `src` を書き換えて発生するチラつきやデコード遅延を防ぐため、裏で非表示の `Image` オブジェクトに先行して画像をデコード（`onload` / `onerror` 待機）させ、完了した瞬間に `replaceChildren()` でコンテナごとDOMをすり替えるゼロ遅延レンダリング機構を実装。
* **隣接画像プリロード並列化:** 表示中の画像の前後の隣接画像を `Promise.all` により並列プリフェッチ。ループ表示時のインデックス循環解決を含め、高速な画像切り替え時のレスポンスを確保。

### 2.2 ファイルシステム & ドライブ検出
* **ネイティブドライブ検出:** Windows APIの `GetLogicalDrives` を直接呼び出し、接続されている物理ドライブ（`A:\`〜`Z:\`）のビットマスクをミリ秒以下で検出して動的にツリーへ展開。
* **遅延読み込みツリー:** フォルダ構造は一度に読み込まず、ユーザーの展開操作（トグルクリック/キーナビ）に応じて非同期で下位フォルダを取得（遅延ロード）。先回りで「さらに1つ下の階層（孫）」にサブフォルダが存在するかを裏でチェックし、中身が空であれば展開用矢印アイコンを自動的に非表示にする最適化を実装。

### 2.3 タブシステム（マルチタスク）
* **複数タブ管理:** 複数タブの並列起動、個別閉じ（中クリック対応）、コンテキストメニューからの「タブ複製」「他のタブをすべて閉じる」「右側のタブをすべて閉じる」を完全にサポート。
* **状態の永続化:** タブごとの閲覧パス、スクロール位置、適用されているソート/検索クエリ設定を `tabsState` として LocalStorage に常時保存し、アプリ再起動時に寸分狂わずに前回の状態を復元。
* **スクロール位置の保存と復元:** タブを切り替える直前に `updateCurrentTabState()` を呼び出し、各タブオブジェクトのプロパティにスクロール位置（`scrollTop`）を保存。復元時は、`appState.savedScrollTopGrid` / `appState.savedScrollTopList` に目標値をセットし、**仮想スクロールのレンダリングサイクル内（`renderGrid` / `updateVirtualList`）にて、コンテナ全体の高さが算出された直後に同期的に復元**する。これにより不要な二重レンダリング（キューイングの暴走）や遅延を防ぎ、遅延のない位置復元を実現している。

### 2.4 ナビゲーション・履歴
* **進む・戻る履歴:** タブごとに移動履歴（`history` 配列と `historyIndex`）を保持。マウスの拡張ボタン（進む・戻るボタン）のフックにも対応。
* **コンテキスト履歴メニュー:** 戻る・進むボタンの「長押し（300ms）」または「右クリック」により、履歴内のフォルダ名とお気に入りアイコン・カラーが反映されたメニューをポップアップ表示し、一階層ずつ戻る手間を省いて目的の履歴へジャンプ可能。

---

## 3. メタデータパース & インスペクター

### 3.1 網羅的なAI生成メタデータ解析ロジック
画像バイナリを自前で解析し、主要なAI生成プログラミング形式を完全に網羅。
1. **PNG形式:** `tEXt` / `iTXt`（Zlib圧縮対応）チャンクから Description, Comment, parameters などを抽出。ComfyUIの `workflow` / `prompt` 構造のフォールバックも実装。
2. **WebP / JPEG形式:** バイナリをメモリマップ（`memmap2`）で展開し、TIFF IFD構造をパースして `ImageDescription` や `UserComment`（UNICODE/ASCIIヘッダースキップ処理付き）、`Software` を高速抽出。
3. **Stealth形式 (隠しメタデータ):** EXIF等に痕跡がない場合、画像のアルファチャンネル（最下位ビット）から Column-Major Order (x -> y) でビットを抽出し、MSB first でバイト配列に変換。`stealth_pngcomp` (Gzip圧縮) および `stealth_pnginfo` (非圧縮) をデコードしてプロンプトを復元。
4. **NovelAI V4 / V4.5 特殊対応:** JSON内のマルチキャラクタープロンプト（`v4_prompt/caption/char_captions`）およびネガティブ側を自動パースし、フロントエンド側で `characterPrompts` 配列（`prompt`, `uc`）に再構造化して保持。
5. **A1111 / WebUI形式フォールバック:** parameters 内の `Steps: ` 等の文字列パターンを検出し、自動的に `rawParameters` フィールドに格納してパース。

### 3.2 フロントエンド・インスペクター & DOM Pool
* **DOM Pool（要素の再利用）:** インスペクターに大量のプロンプトタグやパラメータを描画する際、メモリ解放（GC）によるガタつきを排除するため、`inspectorSectionPool` および `inspectorTagPool` を実装。画面更新時はDOM要素を一度も破棄せず、`display = 'none'` による非表示化と中身の `replaceChildren()` による書き換えだけで要素を高速に再利用。
* **複数選択サマリー表示 (`renderMultipleSelectionSummary`):** 画像が2件以上選択された場合、単一画像のメタデータ表示から「複数選択概要」へ自動切り替え。選択件数、フォルダ全件に対する比率（％）、および一括実行可能なショートカット操作（1〜5キーで一括レーティング、Deleteキーで一括ゴミ箱移動、Ctrl+Cでパスコピー等）のガイドをパラメータボックスと同一のUIコンポーネントで統一表示。
* **パラメータバッジ色分け:** パラメータのサブ属性を解析し、`Inpainting`（青）、`Vibe Transfer`（紫）、`Character Reference`（オレンジ）、`Image to Image`（緑）の専用バッジを割り当て、一目で生成アプローチを識別可能。
* **カラートークンの統一:** バッジ、選択枠、境界線、テキスト透過色などのスタイル定義はすべて `style.css` の CSS カスタムプロパティ（`--glow-gold`, `--danger-red`, `--border-color` 等）で一元管理。
* **検索ワード連動ハイライト:** メイン画面の検索バーに入力された複数キーワード（カンマ区切り）と完全に連動し、インスペクター内の該当単語（タグ）の枠線と背景をゴールドに発光させ、テキスト自体にも `<mark>` タグによるハイライトを適用。
* **タグのクリップボード操作:** インスペクター上のタグをクリックすると、そのタグ文字列をクリップボードにコピー可能。`Ctrl + クリック` を行った場合は、該当タグを「追加コピー（Append）」するが、**WebView2の `navigator.clipboard.readText()` によるセキュリティプロンプト表示を回避するため、クリップボードの最新状態はJSの内部変数（`_lastCopiedTags`）で自己管理**し、カンマ区切りで結合した文字列を再書き込み（`writeText`）する。操作後には追加・上書きされた旨をトースト通知で視覚的にフィードバックする。（※視覚的ノイズを減らすため、タグホバー時の「コピー」ツールチップは意図的に廃止されている）

---

## 4. サムネイル & パフォーマンス最適化

### 4.1 ハイブリッド・サムネイル生成パイプライン
サムネイル処理パイプラインは独立モジュール（`src/renderer-thumbnails.js`）として分離され、Web Worker プールと Rust バックエンドを組み合わせた往復バッチシステムとして稼働。
1. **ファストパス表示:** ディレクトリ展開時、Rust側がSQLiteを検索し、すでにキャッシュが存在するファイルは `generate_thumbnail` IPC コマンドが返す Tauri Asset URL をそのまま画像の `src` に使用。
2. **Web Workerプールによる並列生成とフェイルセーフ:** キャッシュが未生成の画像は、`ThumbnailWorkerPool`（最大8スレッド同時実行）によってメインスレッドをブロックせずに並列生成される。Chromium 109環境で一部の非互換画像が `createImageBitmap` や `fetch` を永続的にハングアップさせる問題に対処するため、**内部で `AbortController` と `Promise.race` を用いた「5秒間の絶対タイムアウト機構」**を組み込んでおり、タイムアウト時は即座に代替アイコンへフォールバックして後続の生成キューの詰まりを防止する。さらに定期的なスタック検知タイマー（ドラッグ＆ドロップ中は誤検知防止のためガード付きでスキップ）が稼働し、不測の滞留タスクを自己修復する。
3. **即時描画（Blob URL）と非同期保存の分離:** サムネイル画像のデコード完了後、DOMの `src` にはメモリ効率の良い `Blob URL`（`URL.createObjectURL`）を即座に適用してUIに表示させる。同時に、Base64変換 と Rust（SQLite）への保存処理をバックグラウンドの非同期Promiseとして分離して実行する。保存完了後、一時的なBlob URLは解放され、軽量な SQLite 経由のURLに差し替えられる。
4. **表示中アイテムの即応枠（リザーブ）と並行制御:** ユーザーが高速でスクロールし、画面外に通り過ぎた大量のアイテムがキューに溜まった場合でも遅延を防ぐため、**全8枠の並行実行枠のうち、最大4枠を「現在画面外のアイテムのバックグラウンド生成」に制限**している。これにより、残りの4枠は**「いま画面に表示されているアイテム」のために常に即応枠として予約（リザーブ）**され、スクロールを止めた際に最優先で表示される。
5. **キューの即時破棄（Abort）機構:** 別フォルダへの移動や「ソート順の変更」が行われた際は、現在バックグラウンドで実行中・待機中のすべてのサムネイルタスクに対して `AbortController` で即座に中断シグナルを発行しキューを破棄する。これにより、古い画面状態のタスクが新しい画面状態の処理をブロックする遅延を完全に防ぐ。
6. **キャッシュ再構築の即時反映:** ユーザーが「選択項目のキャッシュ再構築」を実行した際、再生成された画像をブラウザのハードキャッシュを回避して即座に画面へ反映させるため、画像URLにキャッシュバスター（`&_cb=日時`）を付与し、画面のリロードなしで即座にUIをアップデートする。

### 4.2 動画対応・ストリーミングサーバー
* **ネイティブ動画サムネイル抽出:** MP4動画ファイルのサムネイルを要求された場合、Windows環境では組み込みの `ffmpeg.exe`（存在する場合、非ウィンドウモード `CREATE_NO_WINDOW` で実行）または Windows ネイティブの `IShellItemImageFactory`（Shell API）を呼び出し、動画の1秒目のフレームからダイレクトにビットマップ（BGRA→RGBA変換）を抽出し、高速にPNGデータとしてフロントエンドに返却。
* **ローカルHTTP動画サーバー:** 大容量の動画（MP4）をWebView2でスムーズに再生するため、Rust側でランダムな空きポートを使って `TcpListener` をホストする軽量なオンデマンドHTTPサーバーが稼働。
* **2MB制限チャンクストリーミング:** 動画再生時、ブラウザ側からの `Range: bytes=...` ヘッダーを解析。バックエンド側でファイルのシーク位置（`SeekFrom::Start`）を調整し、**1回のリクエストに対する返却サイズを最大2MBに厳格に制限**して部分配信（`206 Partial Content`）を行うことで、大容量動画でもメモリを圧迫せずに即時シーク・再生可能。
* **イベント駆動型シークバー同期:** 動画シークバーの位置更新は、従来のポーリング（`setInterval`）を廃止し、`<video>` 要素の `timeupdate` イベント委譲による駆動方式を採用。動画再生中のCPU使用率を大幅に低減。

### 4.3 仮想スクロール（バーチャルスクロール）
* **通信ボトルネックの排除:** フォルダ内に数万枚の画像があっても、Rust側で状態（`all_files` / `filtered_files`）をSource of Truthとして保持し、JS側には総件数（`total_count`）のみを通知。JS側の仮想スクロールの描画領域に必要な分（例：100件ずつ）だけ、`get_items(offset, limit)` IPCコマンドでピンポイントにスライスして取得。
* **DOM要素へのO(1)アクセス最適化:** 各サムネイルDOMをパス文字列をキーとする `uiManager._domByPath` Mapで管理。レーティングの更新やシャッターフラッシュ適用など、特定のDOM要素を操作する際に `querySelector` によるO(N)のツリー探索を完全に排除し、`O(1)` で要素を特定する構造を採用。
* **スクロール更新の排他ロック制御 (`_runWithUpdateLock`):** 高速マウスホイール操作時に複数のスクロール更新処理が同時に走るのを防ぎ、更新中に発生した最新のリクエストをキューイングして後続で1回だけ実行するロック機構を実装。多重描画によるカクつきや不要なReflowを抑止。
* **Reflow/Repaintの徹底排除:** 要素の `textContent` や `style` 更新は必ず現在値と比較し（`_cachedRating` 等を利用）、変更がある場合のみ書き込む。これにより無条件な書き込みによるブラウザの不要なReflowを抑制。

---

## 5. スマートフォルダ & 高度なフィルタ

### 5.1 評価（レーティング）システム
* SQLite上の独立した `ratings` テーブルで管理され、ファイルパスを主キーとして1〜5の数値を保存（0はレコード削除）。
* レーティングの変更は即座にフロントエンドのグリッド上のバッジ、およびリストビューの行にリアクティブに反映。仮想スクロールのバッファ領域内にキャッシュされているDOM要素に対しても、`_domByPath` による O(1) アクセスと `_cachedRating` のチェックを経て再描画（Reflow）を誘発することなく即座に書き換えが行われ、ユーザーがスクロールせずとも状態が常に同期される設計となっている。

### 5.2 スマートフォルダの検索条件・演算子仕様
ユーザーがUI上で自由に組み合わせ可能な条件ルール（`SmartFolderRule`）は、バックエンド側で動的SQLクエリに変換され、インデックスおよびFTS5仮想テーブルを活用して高速にフィルタリングを実行。

| 条件タイプ (`type`) | 対応演算子 (`operator`) | 処理・計算ロジック |
| :--- | :--- | :--- |
| **prompt** | `contains` / `not_contains` | **FTS5 全文検索:** `c.rowid IN/NOT IN (SELECT rowid FROM cache_fts WHERE searchable_prompt MATCH ?)`<br>※構文エラー時やフォールバック時はエスケープ付き `searchable_prompt LIKE ? ESCAPE '\\'` を使用 |
| **negative_prompt** | `contains` / `not_contains` | **FTS5 全文検索:** `c.rowid IN/NOT IN (SELECT rowid FROM cache_fts WHERE searchable_negative_prompt MATCH ?)`<br>※フォールバック時は `searchable_negative_prompt LIKE ? ESCAPE '\\'` を使用 |
| **source** | `==` / `!=`<br>(`contains` / `not_contains`) | UI上では一致・不一致を選択。FTS5 MATCH 検索または `searchable_source LIKE ? ESCAPE '\\'` による評価 |
| **rating** | `>=`, `<=`, `==`, `!=` | `coalesce(r.rating, 0)` を用いた数値評価。<br>**INNER JOIN 最適化:** 条件結合が `all` かつ評価値が 1 以上の正の条件（`>=`, `>`, `==`）の場合、`ratings r INNER JOIN cache c` に自動切り替え、インデックス結合で高速化 |
| **width** / **height** | `<=`, `==`, `>=` | `c.width` / `c.height` に対する数値評価 |
| **aspect_ratio** | `portrait` | 縦長画像判定公式: `(width * 100 < height * 95)` |
| | `landscape` | 横長画像判定公式: `(width * 100 > height * 105)` |
| | `square` | 正方形判定公式: `(width * 100 >= height * 95 AND width * 100 <= height * 105)` |
| **path** | `in_folder` | 指定フォルダの**直下のみ**を対象とする判定:<br>`c.path LIKE ? AND c.path NOT LIKE ?/% AND c.path NOT LIKE ?\%` |
| | `under_folder` | 指定フォルダの**配下すべて（サブフォルダ含む）**を対象:<br>`c.path LIKE ?%` |

* **ソート順とオプティマイザ制御:** スマートフォルダ一覧クエリの末尾には `ORDER BY +c.mtime DESC` を付与。単項プラス演算子（`+`）により、SQLiteのクエリオプティマイザが意図せず mtime インデックスを強制スキャンして結合効率を落とす事態を回避。
* **特殊組み込みID:** 条件が空の特殊フォルダとして、`fav_5`（星5）、`fav_4_plus`（星4以上）、`history`（`last_accessed` 降順の直近100件）のファストパスを完備。
* **実ファイル非依存動作:** スマートフォルダの読み込み時は、ファイルシステム上の実ファイルの存在有無をチェックせず（`is_file`の同期的確認をスキップ）、SQLiteキャッシュ内の情報のみをベースに高速一覧化。ファイルが存在しない古いキャッシュであっても、保存されていた `size` や `mtime` を遅延読み込みでそのまま表示可能（テストにより挙動を保証）。

---

## 6. ファイル・システム操作 & 堅牢性

### 6.1 高度なドラッグ＆ドロップ（移動・コピー・衝突解決）
* アプリ内のツリーやグリッドから他のフォルダ、もしくはお気に入りバーへのD&Dを完全制御。
* **衝突検出（`check_conflicts`）:** ファイルドロップ時、バックエンド側で非同期にターゲットディレクトリ内の同名ファイルの有無を一括チェック。衝突を検知した場合、フロントエンド側にモダンな「衝突解決ダイアログ」を表示し、ユーザーは「上書き」「スキップ（重複を除外して継続）」「キャンセル」を選択可能。
* **インテリジェント動作判定:** ドロップ先のドライブ（ルート）と移動元のドライブが同じであればデフォルト動作を「移動（`move`）」、異なるドライブ間であれば「コピー（`copy`）」に自動で切り替え（Ctrlキーで強制コピー、Shiftキーで強制移動）。

### 6.2 堅牢なファイルウォッチャー & キャッシュライフサイクル管理
* **変更監視ポーリング:** 外部のエクスプローラやブラウザからの画像保存、ゴミ箱からの復元を検知するため、バックエンド側で1500ms間隔の軽量なフォルダポーリングスレッドを常時稼働。
* **自動クリーンアップ仕様:** ウォッチャーがファイルシステム上での「ファイル削除」を検知した際、あるいはユーザーがアプリ内からファイルをゴミ箱（`trash` クレート経由）に移動した際、そのファイルに関連付けられていた SQLite 内のサムネイルバイナリおよびメタデータキャッシュ、さらに古いファイルベースのキャッシュ（`Thumbnails/*.jpg`, `Metadata/*.json`）を**ハッシュキーベースでストレージから即座に自動完全削除**。不要な残滓を残さない自動クリーンアップ機構により、ディスク容量の浪費を防止。
* **キャッシュの精査と修復 (`audit_cache`):**
  1. **整合性スキャン:** キャッシュ内の全レコードを走査し、実ファイルが存在しないレコードや、大文字小文字・UNCパス正規化による重複レコードをトランザクション内で一括削除。
  2. **プロパティ自動補完:** レコード内の幅・高さ・ファイルサイズ・mtime・ctime に欠落がある場合、ファイルメタデータやバイナリヘッダーから自動補完。
  3. **サムネイル非同期生成:** サムネイル未保持レコードを抽出し、DBトランザクション解放後にUIスレッドをブロックしないバックグラウンド処理として1件ずつ安全に生成・保存。処理中はフロントエンドへ `audit-progress` イベント（現在件数、総件数、削除件数、修復件数）を定期発行。
  4. **領域解放とWAL切り詰め:** 全工程完了後、`VACUUM`、`ANALYZE`、および `PRAGMA wal_checkpoint(TRUNCATE);` を実行してメインDBの断片化解消と `-wal` ファイルの0バイト化（完全フラッシュ）を実施。

### 6.3 タイムトラベル（Undoスタック）
* アプリ内で行われた操作履歴を `undoStack`（配列）にスタック。ユーザーが `Ctrl + Z` を押した際、直前の操作に応じて逆方向のネイティブ処理を実行する。
  * `RENAME_FOLDER` / `RENAME_FILE` → 元の名前にリネーム
  * `MOVE_FILE` → 元のディレクトリに移動
  * `COPY_FILE` → コピーによって生成されたファイルをストレージおよびRust状態から物理削除
* 取り消しが成功すると、UIとキャッシュが自動的に再同期される。

### 6.4 ソートドロップダウン
* **サムネイルコントロールバー内のクイックソート:** `#thumbnail-controls` バー内に常時表示されるカスタムドロップダウン（`#sort-select-container`）から、ソート基準と順序を1アクションで切り替え可能。選択肢は右クリックメニューのソートサブメニューと完全に同じ並び順（名前→拡張子→幅→高さ→比率→サイズ→更新日時→レーティングの各昇順/降順、全16項目）。
* **同期:** テーブルヘッダーのクリックでソートが変わった場合にもドロップダウンのラベルが `updateSortSelectDropdown()` により自動同期される。
* **展開方向の動的判定:** クリック時にウィンドウ下端までの残り高さを計測し、メニューが収まらない場合のみ上方向展開（`open-up`クラス）に切り替える。

### 6.5 モーダル・ダイアログの共通UX設計
* **ヘッダー閉じるボタンの統一:** お気に入り編集、スマートフォルダ編集、パラメータDiff、各種確認ダイアログのヘッダー右上に、統一デザインの「閉じる（×）」SVGボタンを配置。
* **ヘッダー境界線の整理:** 各ダイアログヘッダー下部の不要な下線境界線（`border-bottom`）を撤廃し、クリーンで視認性の高いスタイルに統一。
* **Escキーによる安全な終了:** ダイアログ表示中に `Esc` キーを押下した場合、またはダイアログ外のオーバーレイをクリックした場合、インラインスタイル等の残留を防止しつつ安全にクローズ処理を実行。

### 6.6 主要なグローバルショートカット・インタラクション一覧
* `ダブルクリック` / `Enter`: 選択中のサムネイルから独立ビューアーウィンドウを起動。
* `0` 〜 `5`: 選択中の画像にレーティング（星の数）を設定 / 解除（複数選択対応、UI楽観的更新）。
* `F1` / `H`: 操作ガイド（ヘルプオーバーレイ）の表示 / 非表示。
* `D`: 2枚の画像を選択中の場合、プロンプト差分比較モーダル（Diff）を起動。
* `A`: 独立ビューアー起動時、デスクトップの作業領域に合わせて横一列に素早く自動整列。
* `F2`: 選択中のファイル/フォルダの名前変更ダイアログを表示。
* `F5`: 現在のフォルダの再読み込み（リフレッシュ）。
* `Delete`: 選択中のファイル/フォルダを安全にゴミ箱へ移動。
* `Ctrl + Z`: 直前に行ったリネーム・移動・コピー操作の取り消し（Undo）。
* `Ctrl + C`: 選択中の画像をクリップボードに物理コピー。コピー成功時、リスト行またはサムネイルアイテムの可視領域に対して**「シャッターフラッシュ（不透明度0.5→0の白レイヤーエフェクト）」を適用**し、視覚的な操作感を向上。
* `Ctrl + A`: 現在のフォルダ内のすべての画像を全選択。
* `Ctrl + F`: 検索キーワード入力欄に即座にフォーカス移動し全選択。
* `Alt + ← / →`: フォルダ移動履歴の「戻る」 / 「進む」。
* `Ctrl + Tab` / `Ctrl + Shift + Tab`: 左右のタブへの高速切り替え。
* `Esc`: モーダル、コンテキストメニュー、カスタム選択ドロップダウン、ヘルプ画面の安全なクローズ。
* `矢印キー (↑↓←→)` / `PageUp` / `PageDown` / `Home` / `End`:
  * **グリッドビュー:** コンテナ幅とアイテム寸法から1行の列数を動的に算出し、上下キーによる2次元移動、PageUp/Downによるページ単位移動を実現。
  * **フォルダツリー:** ツリー内フォーカス時、上下キーでフォルダ選択移動、左右キーで階層の展開/折りたたみ、親フォルダへの移動を実行。
  * **Shift + ナビゲーションキー:** 直前選択位置からの連続範囲選択（Shift選択）。

---

## 7. 今後の開発に向けたアーキテクチャメモ (TODO候補)
1. **スマートフォルダ条件の追加拡張:** 本仕様書に定義された動的SQL構築ロジック（`build_smart_folder_query`）および FTS5 連携をベースに、「Model名」「Sampler名」「CFG Scale」「Seed値」などの各種生成パラメータをSQLiteにカラム追加（またはJSONインデックス抽出）し、さらに細かな条件指定（演算子）を行えるように拡張する。
2. **メタデータ抽出フォーマットの継続的追従:** 新興の生成AIモデルやワークフロー（最新バージョンの ComfyUI カスタムノード、新規 WebUI フォーク等）が出力する独自メタデータ形式の解析ロジックを拡充する。

## 8. 開発運用・AIアシスタント連携ルール
1. **AIの自動コミット禁止ルール:** AI生成コードを主体として開発を進める際、AIがユーザーに無断で `git commit` を自動実行することはシステムの重大な運用ルール違反となる（`AGENTS.md` の「絶対遵守事項」に基づく）。
2. 仕様書および機能要件の実装後、AIは変更をワーキングツリーに残し、ユーザーが実機で動作テスト・レビューを終えたのちに手動でコミットするフローを徹底する。

### 8.1 コードコメントとドキュメンテーション品質規範
* **場当たり的・カジュアルなコメントの排除:** 不具合修正時の一時的なチケットIDや「〜対策」等の短絡的コメントは一切残さず、修正理由をブラウザのレンダリングパイプライン（Chromium 109 / WebView2 同期Reflow）、OS API（Windows 8.1 論理ドライブビットマスク）、非同期並行排他制御（`_runWithUpdateLock`）などの**客観的な技術的根拠・設計意図としてコメントに昇華**する方針を徹底。
* **Rust / JS 双方向でのドキュメンテーション標準化:**
  * **Rust (`src-tauri/src/main.rs`):** クレートレベル（`//!`）でmimalloc、SQLite WAL/mmap、xxh3_64、jwalk並列探索、2MBチャンク配信、Window Pool再利用などの基幹アーキテクチャを体系化。主要Tauriコマンド（`load_directory`, `open_viewer`, `save_thumbnail`, `parse_metadata`, `build_smart_folder_query` 等）には `///` doc comments を完備。
  * **JavaScript (`src/*.js`):** `renderer-thumbnails.js`（Workerプール/タイムアウト/Blob URL即時描画）、`renderer-ui.js`（仮想スクロール/DOM Pool/排他ロック）、`viewer.js`（二重バッファリングDOM Swap/先行プリロード/Unsharp SVGフィルター）、`renderer.js`（ライフサイクル/グローバルショートカット統括）の各モジュールヘッダーおよび主要関数にJSDocと設計意図を網羅。
* **過度な誇張表現・絵文字の厳禁:** コメント内であっても「一瞬」「神速」「爆速」等の過度な誇張表現や絵文字（⭐など）は使用せず、客観的で落ち着いた技術的表現を用いる。